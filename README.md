Simple image host/sharer which is intended to work with apps such as ShareX.

> Rename `config.example.json` to `config.json`, then run `node .`.

*Note that this project is intended for personal use only.*
